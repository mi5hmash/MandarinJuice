namespace MandarinJuiceCore.GamingPlatformsFactory;

public enum GamingPlatformEnum
{
    // Numbers are used in the appsettings.bin file, so they should not be changed to maintain backward compatibility with existing attributes.
    Other = 0,
    Steam = 1,
    Epic = 2
}